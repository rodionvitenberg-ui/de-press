# PROGRESS — the two-agent journal

Read `MAINPLAN.md` before any task. The entry format lives there too.

Do not start Track B until Gate 0 = done.

---

## 2026-08-29  agent=A  id=Gate0
status: done
branch: main (ff-merge feat/help-human-ai)
commit: 2ca15ab
notes: MAINPLAN.md + PROGRESS.md on main. Branches feat/agent-a-helper-ops and feat/agent-b-companion-hosts created from main. UI click-through of /help still for a human (no browser tools). API E2E already passed (create/accept/send/skip/cancel/AI). Agent B may start B1.
files: MAINPLAN.md, PROGRESS.md

## 2026-08-29 05:47  agent=B  id=B1
status: started
branch: feat/agent-b-companion-hosts
commit: d17f44d
notes: SSE POST /api/v1/ai/support/stream (old POST /ai/support stays) + companion IndexedDB store (DB v2) + CompanionPane streaming/wipe UI.
files: backend/apps/ai/gateway.py, backend/apps/ai/services.py, backend/api/v1/ai.py, backend/apps/ai/tests/*, apps/browser/src/core/memory/db.ts, apps/browser/src/core/api/client.ts (append only), apps/browser/src/features/help/CompanionPane.*, i18n companion.*


## 2026-08-29  agent=A  id=A1
status: done
branch: feat/agent-a-helper-ops
notes: Help-chat message report without Story. Report.story nullable; XOR story|message; unique open help report per message×actor. Dashboard/admin tolerate null story. Did not stage Agent B files (ai/gateway.py, ai/services.py).
files: backend/apps/moderation/**, backend/api/v1/moderation.py, apps/browser/CAPABILITIES.md

## 2026-08-29  agent=A  id=A2
status: done
branch: feat/agent-a-helper-ops
notes: Dialogue Request waits for Helper (awaiting_helper) before author inbox. contract:ready GET/POST /api/v1/moderation/dialogue-requests. Left B1 WIP unstaged (ai/*, CompanionPane, memory/db.ts). client.ts also had B stream helpers in the same working tree — kept additive methods, did not revert them.
files: backend/apps/dialogue/**, backend/apps/notifications/models.py, backend/api/v1/moderation.py, ChatList.tsx, i18n helper/review, client.ts
api: GET /api/v1/moderation/dialogue-requests
api: POST /api/v1/moderation/dialogue-requests/{id}/approve
api: POST /api/v1/moderation/dialogue-requests/{id}/reject

## 2026-08-29  agent=A  id=A3
status: done
branch: feat/agent-a-helper-ops
notes: One-time Helper invite. Staff/Helper POST /api/v1/helper-invites; candidate POST …/{token}/accept with pledge. No open self-signup. Invite list UI left for A4.
files: backend/apps/identity/**, api/v1/helper_invite.py, HelperJoin.tsx, UserMenu.tsx
api: POST /api/v1/helper-invites
api: GET /api/v1/helper-invites/{token}
api: POST /api/v1/helper-invites/{token}/accept

## 2026-08-29  agent=A  id=A4
status: done
branch: feat/agent-a-helper-ops
notes: /helper tabs Clouds + Summary (dashboard metrics, recent reports including help-chat, own invites). Help/dialogue queues stay in /chat.
files: HelperQueue.tsx, HelperQueue.module.css, helper_invite list GET
api: GET /api/v1/helper-invites

## 2026-08-29  agent=A  id=A5
status: done
branch: feat/agent-a-helper-ops
notes: Helper duty toggle. Default off. Notify+inbox Help Request and dialogue-review only when is_helper && is_on_duty && is_active. Accept/skip still allowed for is_helper. Seed helper@de-press.local on duty. New invitees stay off duty. Presence/instant match left for A6. Isolated in .worktrees/agent-a.
files: identity is_on_duty, help.py, services.py review notify, HelperQueue, UserMenu, ChatList poll gate
api: POST /api/v1/me/helper-duty
api: GET /api/v1/me + is_on_duty

## 2026-08-29  agent=A  id=A6
status: done
branch: feat/agent-a-helper-ops
notes: Instant match when an on-duty Helper has pinged within 45s (least-recent). Else A5 queue. Presence booleans only, no counts/names. Heartbeat from /chat and /helper, paused in Anti-Panic. Wait copy: at screen / on duty / nobody. No 30s promise.
files: dialogue/presence.py, help.py create, HelpWaitPane, ChatList/HelperQueue heartbeat
api: GET /api/v1/help/presence
api: POST /api/v1/help/heartbeat

## 2026-08-29  agent=B  id=B1
status: done
branch: feat/agent-b-companion-hosts
notes: SSE endpoint POST /api/v1/ai/support/stream (events meta → delta* → done, error mid-stream; headers no-cache + X-Accel-Buffering: no). Old POST /ai/support untouched; validation shared via _prepare(); crisis short-circuit sends CRISIS_REPLY_RU as a single piece (no typewriter); offline gateway streams as one chunk. Companion chat history now in IndexedDB DB v2 (store companion_messages), wiped together with everything by wipeAllMemory; wipe button in CompanionPane with its own confirmation. Shared files (client.ts, i18n ru/en/types) — only additive blocks within own sections. Tests: backend apps/ai 13 passed, browser vitest 43 passed, tsc+vite build ok.
files: backend/api/v1/ai.py, backend/apps/ai/gateway.py, backend/apps/ai/services.py, backend/apps/ai/tests/test_services.py, backend/apps/ai/tests/test_api_stream.py, apps/browser/src/core/api/client.ts, apps/browser/src/core/memory/db.ts, apps/browser/src/features/help/CompanionPane.tsx, apps/browser/src/features/help/CompanionPane.module.css, apps/browser/src/core/i18n/types.ts, apps/browser/src/core/i18n/messages/ru.ts, apps/browser/src/core/i18n/messages/en.ts, PROGRESS.md
api: POST /api/v1/ai/support/stream (SSE)

## 2026-08-29 07:05  agent=B  id=B2
status: started
branch: feat/agent-b-companion-hosts
commit: b8ac275
notes: Mini App HELP parity (apps/mini-app/** only, no imports from browser): HELP cards → create request, /help/wait, companion /help/ai (SSE + IndexedDB DB v2 + wipe), help-inbox rows in ChatList for Helpers, startapp help_wait/help_ai. helper_join skipped — A3 not in main at this branch point. Ops note: agent A switched the shared worktree to their branch mid-session and committed A3 (5c1b5be); A's uncommitted "A3 started" PROGRESS entry was stashed by B as stash@{0} ("agent A pending A3-started PROGRESS entry...") — A should pop it on feat/agent-a-helper-ops.
files: apps/mini-app/src/core/api/client.ts (append), apps/mini-app/src/core/api/types.ts, apps/mini-app/src/core/memory/db.ts, apps/mini-app/src/core/host/startParam.ts, apps/mini-app/src/App.tsx, apps/mini-app/src/features/help/*, apps/mini-app/src/features/chat/ChatList.*, apps/mini-app/src/core/i18n/*, PROGRESS.md

## 2026-08-29 07:45  agent=B  id=B2
status: done
branch: feat/agent-b-companion-hosts
notes: Mini App HELP parity copied (no shared package, no imports from apps/browser — verified by grep). HelpPane: human/AI path cards (POST /api/v1/help/requests → /help/wait). New HelpWaitPane: polls GET /help/requests/mine every 4s (paused in Anti-Panic), cancel, open chat or AI. New CompanionPane (/help/ai): SSE stream via api.aiSupportStream, history in IndexedDB DB v2 (companion_messages), wiped together with everything by wipeAllMemory; wipe button in CompanionPane with its own confirmation. ChatList: gray Help-inbox rows for Helpers (accept → chat, skip; pending-only; polling paused during Anti-Panic). startapp deep-links: help_wait/help-wait/wait → /help/wait, help_ai/help-ai → /help/ai; routes /help/wait and /help/ai added. helper_join deferred — A3 not in main at branch point. Build: tsc -b && vite build ok (163 modules). During the task agent A switched the shared worktree twice; B-zone edits survived checkouts and were committed on this branch with explicit pathspecs (apps/mini-app PROGRESS.md only).
files: apps/mini-app/src/core/api/types.ts, apps/mini-app/src/core/api/client.ts (append), apps/mini-app/src/core/memory/db.ts, apps/mini-app/src/core/host/startParam.ts, apps/mini-app/src/App.tsx, apps/mini-app/src/features/help/HelpPane.tsx, apps/mini-app/src/features/help/HelpPane.module.css, apps/mini-app/src/features/help/HelpWaitPane.tsx (new), apps/mini-app/src/features/help/HelpWaitPane.module.css (new), apps/mini-app/src/features/help/CompanionPane.tsx (new), apps/mini-app/src/features/help/CompanionPane.module.css (new), apps/mini-app/src/features/chat/ChatList.tsx, apps/mini-app/src/features/chat/ChatList.module.css, apps/mini-app/src/core/i18n/types.ts, apps/mini-app/src/core/i18n/messages/ru.ts, apps/mini-app/src/core/i18n/messages/en.ts, PROGRESS.md

## 2026-08-29 08:10  agent=B  id=B3
status: started
branch: feat/agent-b-companion-hosts
commit: ca2cc64
notes: Voice pipeline STT→translate→TTS. Backend (my zone): TTS adapters in apps/dialogue/speech.py (OpenAI-compatible /audio/speech, no key → get_tts()=None), thin endpoint POST /api/v1/messages/{id}/tts in api/v1/dialogue.py (ensure_transcript → translate to actor locale → synthesize; 503 = honest offline marker; translation unavailable → speaks original). Mini-app voice bubble: transcribe button + TTS "play translated" (blob playback). Browser VoiceBubble is A's zone — not touched (MAINPLAN allows API+Mini App first).
files: backend/apps/dialogue/speech.py, backend/api/v1/dialogue.py (append), backend/apps/dialogue/tests/test_speech.py, apps/mini-app/src/core/api/client.ts (append), apps/mini-app/src/features/chat/DialoguePage.tsx, apps/mini-app/src/features/chat/DialoguePage.module.css, apps/mini-app/src/core/i18n/types.ts, apps/mini-app/src/core/i18n/messages/ru.ts, apps/mini-app/src/core/i18n/messages/en.ts, PROGRESS.md
api: POST /api/v1/messages/{id}/tts → audio/mpeg | 503 {"detail":"TTS offline: no key on server"}

## 2026-08-29 08:30  agent=B  id=B3
status: done
branch: feat/agent-b-companion-hosts
notes: Voice pipeline STT→translate→TTS. Backend: TextToSpeech protocol + OpenAICompatibleTTS (/audio/speech, mp3) + get_tts() in apps/dialogue/speech.py — no key → None, never fake audio. Endpoint POST /api/v1/messages/{id}/tts (api/v1/dialogue.py, appended): ensure_transcript (STT, reuses existing) → translate_message to payload.lang (actor UI locale; if translation unavailable offline — speaks original transcript honestly) → synthesize → HttpResponse audio/mpeg; 400 not-voice/no-text, 503 = offline marker ("TTS offline: no key on server"). Mini-app voice bubble: "Transcribe" button when no transcript (api.transcribeMessage), "Listen to the translation" (api.messageTts → blob → Audio playback, URL revoked on ended), honest offline marker inline on 503. i18n chat.transcribe/ttsListen/ttsOffline (ru/en/types, append-only). Browser VoiceBubble/DialoguePage untouched (A's zone). Tests: DEPRESS_USE_SQLITE=1 pytest test_speech.py 6 passed (3 new TTS unit tests), test_api_chat_prefs.py 2 passed, manage.py check ok; mini-app tsc+vite build ok.
files: backend/apps/dialogue/speech.py, backend/api/v1/dialogue.py (append), backend/apps/dialogue/tests/test_speech.py, apps/mini-app/src/core/api/client.ts (append), apps/mini-app/src/features/chat/DialoguePage.tsx, apps/mini-app/src/features/chat/DialoguePage.module.css, apps/mini-app/src/core/i18n/types.ts, apps/mini-app/src/core/i18n/messages/ru.ts, apps/mini-app/src/core/i18n/messages/en.ts, PROGRESS.md
api: POST /api/v1/messages/{id}/tts → audio/mpeg | 503 {"detail":"TTS offline: no key on server"}

## 2026-08-29 08:50  agent=B  id=B4
status: done
branch: feat/agent-b-companion-hosts
notes: ADR 0019 (de-press-docs/app/adr/0019-own-mobile-vs-mini-app-desktop-tauri.md): Mini App != own mobile (stays Telegram addon); own mobile = PWA-first with explicit missing-pieces backlog (Web Push/VAPD, optional TWA store chrome, background sync) — no code; own desktop shell chosen = Tauri reusing browser build, deferred per ADR 0016 resource constraints; native/ reserved for agent B, deliberately empty (no native code shipped). Verified facts before writing: browser PWA live (manifest.webmanifest + sw.js registered in prod), native/ does not exist, previous ADR number 0018. Started+done in one commit (doc-only microtask).
files: de-press-docs/app/adr/0019-own-mobile-vs-mini-app-desktop-tauri.md (new), PROGRESS.md

## 2026-08-29  agent=A  id=merge
status: done
branch: main
notes: Local merge of feat/agent-a-helper-ops then feat/agent-b-companion-hosts. Only conflict was PROGRESS.md (concatenated; no entries dropped). client.ts and i18n auto-merged. Mini App still without A3 join / A5 duty / A6 match UI. Feature branches kept.
files: PROGRESS.md

## 2026-08-29 08:14  agent=A  id=A2-A6-miniapp
status: done
branch: main
notes: Mini App browser parity for Track A helper ops (no imports from apps/browser — verified by grep). API: types (Me.is_staff/is_on_duty, HelpPresence, HelperInvite, NotificationKind += dialogue_request_review) + client endpoints (helpPresence, helperHeartbeat, dialogueReviewInbox, approve/rejectDialogueReview, create/list/get/acceptHelperInvite, setHelperDuty). i18n: helper.review*/invite*/join*/tab*/duty*, help.waitMatchedTitle/waitPresence*, notifications.kind.dialogue_request_review (en/ru/types). ChatList: duty-gated review inbox (awaiting_helper, 20s), heartbeat via useHelperHeartbeat(isHelper) — browser contract (single enabled arg, useAntiPanic inside), review blocks with approve/reject. HelperQueue: clouds/summary tabs, duty card+toggle, dashboard metrics/reports, invites list. HelperJoin.tsx (new): /helper/join (token preview, pledge, accept) + /helper/invite (create link); routes wired; start_param helper_join_<uuid> → /helper/join?token= (startParam.ts). UserMenu: duty toggle (is_helper) + invite link (is_helper|is_staff). HelpWaitPane: presence line (helpPresence poll 4s, Anti-Panic paused) + waitMatchedTitle. Backend: stale test fix — test_websocket_receives_live_notification used kind "message" which is inbox-hidden by design (chat unread lives on the dialogue list, _INBOX_HIDDEN since 99723a4), so unread_count event was 0; switched to "dialogue_request" (counted kind). Verify: mini-app tsc -b + vite build ok; browser tsc -b ok + vitest 8 files / 43 tests passed; backend apps/notifications 18 passed. Uncommitted foreign-zone edits (browser StoryPage/i18n, backend support) left untouched.
files: apps/mini-app/src/core/api/types.ts, apps/mini-app/src/core/api/client.ts, apps/mini-app/src/core/i18n/types.ts, apps/mini-app/src/core/i18n/messages/en.ts, apps/mini-app/src/core/i18n/messages/ru.ts, apps/mini-app/src/core/host/startParam.ts, apps/mini-app/src/App.tsx, apps/mini-app/src/components/layout/UserMenu.tsx, apps/mini-app/src/features/chat/ChatList.tsx, apps/mini-app/src/features/help/HelpWaitPane.tsx, apps/mini-app/src/features/helper/HelperQueue.tsx, apps/mini-app/src/features/helper/HelperQueue.module.css, apps/mini-app/src/features/helper/HelperJoin.tsx (new), apps/mini-app/src/features/helper/HelperJoin.module.css (new), apps/mini-app/src/features/helper/useHelperHeartbeat.ts (new), backend/apps/notifications/tests/test_consumers.py, PROGRESS.md

## 2026-08-29 22:24  agent=human  id=A2-rollback
status: done
branch: main
notes: Rolled back A2 helper pre-moderation of Dialogue Requests — requests now go straight to the author. create_request always PENDING, review notify removed; legacy review inbox/approve/reject endpoints kept (old notifications/data still render, new requests skip the gate). Migration 0012 maps awaiting_helper → pending. Review inbox UI removed from browser+mini-app ChatList; dead client methods (dialogueReviewInbox/approve/reject) removed; apps/mini-app/dist/ gitignored. Verified: backend full suite 100% no F/E + test_dialogue_review 2 passed; browser tsc + vitest 43/43; mini-app tsc.
files: backend/apps/dialogue/models.py, backend/apps/dialogue/services.py, backend/apps/dialogue/migrations/0012_release_awaiting_helper.py (new), backend/apps/dialogue/tests/helpers.py, backend/apps/dialogue/tests/test_dialogue_review.py, backend/apps/identity/models.py, apps/browser/src/components/layout/UserMenu.tsx, apps/browser/src/core/hooks/useNotifications.ts, apps/browser/src/core/i18n/messages/ru.ts, apps/browser/src/core/i18n/messages/en.ts, apps/browser/src/features/chat/ChatList.tsx, apps/browser/src/features/helper/HelperQueue.tsx, apps/browser/src/core/api/client.ts, apps/mini-app/src/components/layout/UserMenu.tsx, apps/mini-app/src/core/i18n/messages/ru.ts, apps/mini-app/src/core/i18n/messages/en.ts, apps/mini-app/src/features/chat/ChatList.tsx, apps/mini-app/src/features/helper/HelperQueue.tsx, apps/mini-app/src/core/api/client.ts, .gitignore, PROGRESS.md

## 2026-08-29  agent=human  id=voice-stt-tts-rollback
status: done
branch: main
notes: Rolled back the voice STT→translate→TTS pipeline (user decision: not needed for now, cannot be implemented adequately without keys). Removed: auto-STT on voice send (dialogue voice notes + story voice monologues/comments), ensure_transcript, POST /messages/{id}/transcribe, POST /messages/{id}/tts (B3), STT/TTS adapters + get_stt/get_tts in speech.py (translator stack kept), STT_* settings, mini-app transcribeMessage/messageTts clients, voice-bubble buttons (Transcribe/Listen to the translation) + ttsBusy/ttsError state + i18n chat.transcribe/ttsListen/ttsOffline (en/ru/types) + .voiceActions/.ttsOffline CSS + ChatMessage.transcript type field. translate_message now refuses transcript-less voice messages ("Nothing to translate") instead of translating the "[voice message]" placeholder; mini-app "Translate" action hidden for voice bubbles (browser menu was already text-only). Text-message translate + UI-catalog translation (apps/common/i18n_ui.py) untouched. DB fields transcript/translations/source_lang kept (legacy data, no migration); old transcripts still render via display_text. Docs: README (features/stack/env/priorities), ROADMAP (v0.14 row, P1 row → dropped, Voice items 2/6), .env.example. Tests updated: test_speech.py keeps 3 translator tests (3 TTS tests dropped), voice tests now assert no transcript. Verify: manage.py check ok; DEPRESS_USE_SQLITE=1 pytest test_speech.py+test_services.py+test_api_chat_prefs.py+stories/test_services.py → 37 passed (full dialogue dir exceeds 30s cmd timeout — env known); mini-app tsc -b + vite build ok; browser tsc -b ok; repo grep: no STT/TTS/transcribe leftovers in code.
files: backend/apps/dialogue/speech.py, backend/apps/dialogue/services.py, backend/api/v1/dialogue.py, backend/config/settings/base.py, backend/apps/stories/services.py, backend/apps/dialogue/tests/test_speech.py, backend/apps/dialogue/tests/test_services.py, backend/apps/stories/tests/test_services.py, apps/mini-app/src/core/api/client.ts, apps/mini-app/src/core/api/types.ts, apps/mini-app/src/core/hooks/useChatSocket.ts, apps/mini-app/src/features/chat/DialoguePage.tsx, apps/mini-app/src/features/chat/DialoguePage.module.css, apps/mini-app/src/core/i18n/types.ts, apps/mini-app/src/core/i18n/messages/ru.ts, apps/mini-app/src/core/i18n/messages/en.ts, README.md, de-press-docs/app/ROADMAP.md, .env.example, PROGRESS.md

## 2026-08-29  agent=human  id=frontend-hardening
status: done
branch: main
notes: Design-polish pass, Stage A (skill impeccable-design-polish, audit mode). Project run: daphne :8005 + browser vite :5174 + mini-app vite :5175; vitest 43/43; mini-app tsc+vite build ok; sw.js node --check ok. Fixes: (1) browser sw.js — navigate branch does not cache non-ok responses anymore (a 500 could previously poison the offline shell); (2) mini-app base.css — overscroll-behavior none, iOS zoom guard (pointer:coarse → input/textarea/select 16px), body.antiPanicActive overflow lock (useAntiPanic toggled the class but the CSS rule was missing); (3) mini-app index.html — viewport meta + interactive-widget=resizes-content (parity with browser host). Deferred deliberately: TG viewportStableHeight pinning — blind pinning risks hiding the composer behind the Android keyboard (webview resize is already handled by the height:100% chain); needs live TG QA via tunnel in Stage B. Audit notes: PWA on browser host is complete per MOBILE_PWA.md (manifest, icons, sw strategies, install row in More, PROD-only registration); tokens/base drift between hosts — keep fixes in sync on both sides; aria 118/96; safe-area browser-only (ok for TG host until live QA says otherwise).
files: apps/browser/public/sw.js, apps/mini-app/src/styles/base.css, apps/mini-app/index.html, PROGRESS.md

## 2026-08-29  agent=human  id=help-page-split
status: done
branch: main
notes: Reworked /help for browser host per user request. (1) Help journey routes (/help, /help/wait, /help/ai) render via new HelpChrome wrapper: full-bleed on desktop (>1099px, no rail/tab, own main + AntiPanicOverlay), regular Shell with TabBar on phone/tablet so mobile UX is unchanged. (2) /help is now a full-page gate with two halves: left = AI promo (tag/title/lead + Link to /help/ai), right = human helper — one click (whole half via stretched CTA on desktop) creates a HelpRequest and navigates to /help/wait (existing pipeline: 4s poll of my-help-request + presence line → helper accepts in HelperQueue → dialogue opens in chat). Optional note textarea stays phone/tablet-only. Crisis orienters, resources, safety and guides kept below the fold. (3) /help/ai now spans full page width; inner column capped at 56rem for readability; back button added. i18n: added common.back, help.aiTag, help.humanTag (ru/en/types). Mini-app untouched (phone-only). Helper dashboard design deferred (user: "later"). Verify: tsc -b ok, vitest 43/43, vite build ok; services restarted after session resume (daphne :8005, vite :5174/:5175).
files: apps/browser/src/App.tsx, apps/browser/src/App.module.css, apps/browser/src/features/help/HelpPane.tsx, apps/browser/src/features/help/HelpPane.module.css, apps/browser/src/features/help/CompanionPane.tsx, apps/browser/src/features/help/CompanionPane.module.css, apps/browser/src/features/help/HelpWaitPane.module.css, apps/browser/src/core/i18n/types.ts, apps/browser/src/core/i18n/messages/ru.ts, apps/browser/src/core/i18n/messages/en.ts, PROGRESS.md

## 2026-08-30  agent=human  id=chat-unread-autoread
status: done
branch: main
notes: Fixed stale unread badge on the currently open chat. Root cause: DialoguePage called POST /dialogues/{id}/mark-read only once per dialogue switch; incoming messages arriving while the window was hidden/minimized raised server unread_count, background ChatList poll was paused (refetchIntervalInBackground=false), and on refocus refetchOnWindowFocus delivered a fresh unread_count for the open dialogue — badge persisted until the user switched chats. Fix in DialoguePage: markRead is now a deduped callback (in-flight ref) invoked (1) on dialogue open/switch (previous behavior), (2) when a peer message lands while the page is visible (lastReadMarked ref keyed by message id prevents repeat POSTs from the 4s HTTP-fallback poll), (3) on visibilitychange→visible and window focus with the chat open — badge drops on return. Invalidates ["dialogues"] each time so ChatList/TabBar/Sidebar badges clear immediately. Verify: tsc -b ok, vitest 43/43, vite build ok.
files: apps/browser/src/features/chat/DialoguePage.tsx, PROGRESS.md

## 2026-08-30  agent=human  id=help-gate-trim
status: done
branch: main
notes: Trimmed /help to just the gate per user request: removed the below-the-fold .rest block (crisis/panic card, resources regions, safety paragraphs, guides, feed/patterns footer links). Header + AI half + human half remain; phone/tablet stacking and optional note unchanged; desktop .gate fills the first screen on its own (flex 1 0 auto + min-height), no CSS change needed. Dropped now-unused useAntiPanic import. i18n keys (help.resources, safety.*, guides.*, antiPanic.menuHint) left in messages/types as data-only dead weight, reusable later — no UI references left. Verify: tsc -b ok, vitest 43/43, vite build ok.
files: apps/browser/src/features/help/HelpPane.tsx, PROGRESS.md

## 2026-08-30  agent=human  id=pwa-prod-verify
status: done
branch: main
notes: PWA build verification (deferred from frontend-hardening) — closed. vite build + vite preview :4173: index serves <link rel="manifest">; /sw.js 200 (PROD-only registration in main.tsx); manifest valid — name/short_name de-press, start_url /feed, scope /, display standalone, theme/bg #0c0e12, icons 192/512/maskable-512 all 200; hashed /assets 200. sw.js re-audited against MOBILE_PWA.md: SHELL v1 pre-cache (/ , /index.html, manifest), network-only /api /ws /media /docs /openapi.json, cache-first /assets/*, navigate network-first with cached /index.html fallback (non-ok not cached). Install row in More has 3 states (installed / beforeinstallprompt / iOS manual). Not verifiable locally: real beforeinstallprompt + offline shell on device — pilot QA on the user's phone. Preview left running on :4173 for desktop install check. ROADMAP P1 PWA bridge 🟡→✅.
files: de-press-docs/app/ROADMAP.md, PROGRESS.md

## 2026-08-30  agent=human  id=full-test-pass
status: done
branch: main
notes: Full test pass before deploy prep. browser: tsc -b ok, vitest 43/43, vite build ok; mini-app: tsc -b ok, vite build ok. backend: full pytest 197 passed (was 1 failed: test_publish_voice_http asserted non-empty body — stale expectation from before the STT rollback; offline voice publish is audio + optional caption, body==""; assertion updated to pin current contract). smoke_api.sh: full pass (exit 0) — fixed stale "moderated free-text" step: it sent a second cloud from the same hearer session and hit the one-cloud-per-story rule (HTTP 400 "One cloud per this monologue"); now the free-text cloud is sent from a fresh hearer session and lands as pending, author sees delivered only. daphne :8005 up, health ok (database/redis/channels).
files: scripts/smoke_api.sh, backend/apps/stories/tests/test_api.py, PROGRESS.md

## 2026-08-30  agent=human  id=deploy-kit
status: done
branch: main
notes: Deploy kit for bare VPS (no docker), topology decision: systemd units + nginx, system Postgres/Redis. Artifacts: deploy/depress-api.service (daphne 127.0.0.1:8000, DJANGO_SETTINGS_MODULE=config.settings.production, EnvironmentFile=/etc/depress/depress.env), deploy/depress-celery.service + depress-celery-beat.service (digests), deploy/depress-backup.service+timer (pg_dump -Fc 04:00, 14d retention), deploy/nginx-de-press.conf (same origin: browser SPA /, mini-app /tg/, admin static /static/, media /media/, proxy /api /admin /docs /openapi.json /ws with WS upgrade). .env.prod.example mirrors .env.example names for prod (DEBUG=false, secure cookies via settings.production, CSRF derives from CORS origins). de-press-docs/app/DEPLOY.md runbook: server prep → postgres role/db → venv production.txt → env → migrate/collectstatic/seed_quiet_phrases/createsuperuser → units → nginx+certbot → frontends built locally + rsync → rollout procedure → backup/restore → post-deploy checks (health, smoke_api.sh BASE_URL=…, journalctl). mini-app vite base=/tg/ on build only (conditional on command — dev unchanged); BotFather Menu Button → https://<domain>/tg/. ROADMAP: the "Public pages" row → moved to landing repo; "Pilot ops" row links DEPLOY.md. Verified: mini-app tsc -b + vite build (dist refs /tg/assets/…), systemd-analyze verify (only local-missing-binary warnings), nginx conf not runtime-tested (no nginx on dev box).
files: .env.prod.example, deploy/, de-press-docs/app/DEPLOY.md, de-press-docs/app/ROADMAP.md, apps/mini-app/vite.config.ts, PROGRESS.md

## 2026-08-30  agent=human  id=i18n-dead-keys-cleanup
status: done
branch: main
notes: Browser-only i18n dead-key cleanup after the /help gate trim (4ca397c). Removed from apps/browser/src/core/i18n/{types.ts,messages/ru.ts,messages/en.ts}: top-level guides object, top-level safety object, help.resources array, antiPanic.menuHint. Verified before removal: no t.guides.* / t.safety.* / t.help.resources usage in browser src outside i18n; antiPanic.menuHint referenced nowhere after the trim. Kept: antiPanic overlay keys (menuTitle/breatheHint/groundSteps/...), help gate/wait keys, menu/footer strings (safetyLink etc. — different objects). Mini-app untouched on purpose: its HelpPane still actively uses help.resources, safety.*, guides.*, antiPanic.menuHint (alive there). Verified after: browser tsc -b 0, vitest 43/43, vite build ok (bundle 447.56 kB, dead strings gone), zero leftovers of removed keys in src/core/i18n.
files: apps/browser/src/core/i18n/types.ts, apps/browser/src/core/i18n/messages/ru.ts, apps/browser/src/core/i18n/messages/en.ts, PROGRESS.md

## 2026-08-30  agent=human  id=history-doc
status: done
branch: main
notes: Development history narrative de-press-docs/HISTORY.md (first person, plain language): genesis and rules, pre-git era (v0.0–v0.14 + soft-notify), Next.js → Vite SPA rewrite, platform odyssey (ADR 0013–0019, archived native forks), git init 99723a4, two-agent day 2026-08-29 (A1–A6, B1–B4, MAINPLAN/PROGRESS discipline), both same-day rollbacks (A2 pre-moderation, voice STT/TTS), quiet cuts (rays ADR 0018, clouds-as-gesture ADR 0017, /help gate trim, public pages → landing repo, no Docker, no UI-dictionary "multilingual"), current stage (47 commits, 197 backend tests, deploy kit), rollback ledger table. Linked from de-press-docs/README.md. All facts sourced from PROGRESS/ROADMAP/ADR/git history; no invented dates.
files: de-press-docs/HISTORY.md, de-press-docs/README.md, PROGRESS.md

## 2026-08-30  agent=human  id=i18n-parity
status: done
branch: main
notes: i18n full parity, both clients. Backend: 24h server cache for POST /i18n/ui-catalog (key = langs + sha-256 of sorted source strings; cache hit skips rate limit), endpoint test asserts translator runs once for identical payload. Browser bug fixed: persistLocale fired before translation success (and catch was empty) — failed switch was silently persisted; now persistence happens only after success, catalogUnavailable flag set on failure, honest marker t.locale.unavailable shown in UserMenu langs panel. Strings extracted: ThemeSwitch → t.theme.*, StoryComposer → t.composer.* (types/ru/en extended). QuietPhrases duplicate-cloud detection by ApiError.status === 400 instead of msg.includes("одно") (survives translated server text). Offline stub detection unified on voicePreview.isOfflineTranscript (DialoguePage no longer duplicates prefixes); backend speech.py emits one stable ASCII sentinel "[offline {code}]" for all target langs now, legacy "[офлайн …]" still detected for old rows. Dates follow UI locale everywhere: ChatList, DialoguePage, StoryPage, PatternsPane, AuthorClouds (no more [] or hardcoded ru-RU/en-GB split). Mini-app: dynamic translation parity — ported uiLangs.ts/flatten.ts/catalogCache.ts (own IndexedDB "depress_i18n_tg": same origin as browser app, different source catalogs, must not clobber each other), Locale is now string, resolveUiLang + Telegram language_code (tg language_code=kk etc. now works), RTL dir on <html>, translateUiCatalog appended to client, browser-style context (loading + catalogUnavailable, persist-after-success), picker-with-search + unavailable marker + current-lang opener in UserMenu, same theme/composer string sweep and localized dates. Tests: browser vitest 45/45 (new: catalog budget guard ≤ 499 keys vs backend MAX_KEYS=500 — en catalog was 443 with no guard; applyFlat round-trip), tsc -b + vite build ok both apps. Backend full pytest 198 passed (sqlite test env: DEPRESS_USE_SQLITE=1 DJANGO_SETTINGS_MODULE=config.settings.local — Postgres role lacks CREATEDB, plain pytest can't create test DB). Live no-key smoke (de → 400 → marker) not reproducible on this box: a real AI key is configured, service attempts live translation; the honest-400 path is covered by the endpoint test and the I18nUiError → HttpError(400) mapping. Not touched: apps/mini-app/index.html data-host="browser" copy-paste artifact (separate cleanup).
files: backend/api/v1/i18n.py, backend/apps/dialogue/speech.py, backend/apps/common/tests/test_i18n_api.py, apps/browser/src/core/i18n/{types.ts,messages/ru.ts,messages/en.ts,context.tsx,flatten.test.ts}, apps/browser/src/core/api/client.ts, apps/browser/src/components/layout/{UserMenu.tsx,UserMenu.module.css,ThemeSwitch.tsx}, apps/browser/src/features/feed/{QuietPhrases.tsx,StoryComposer.tsx,AuthorClouds.tsx,StoryPage.tsx}, apps/browser/src/features/chat/{ChatList.tsx,DialoguePage.tsx}, apps/browser/src/features/patterns/PatternsPane.tsx, apps/mini-app/src/core/i18n/{types.ts,index.ts,context.tsx,messages/ru.ts,messages/en.ts,uiLangs.ts,flatten.ts,catalogCache.ts}, apps/mini-app/src/core/api/client.ts, apps/mini-app/src/components/layout/{UserMenu.tsx,UserMenu.module.css,ThemeSwitch.tsx}, apps/mini-app/src/features/feed/{QuietPhrases.tsx,StoryComposer.tsx,StoryPage.tsx}, apps/mini-app/src/features/chat/DialoguePage.tsx, apps/mini-app/src/features/patterns/PatternsPane.tsx, PROGRESS.md

## 2026-08-30  agent=human  id=i18n-browser-fallback
status: done
branch: main
notes: Keyless dynamic UI translation for the browser via the built-in on-device Translator API (Chrome 138+/Edge, free, no API key). New apps/browser/src/core/i18n/clientTranslate.ts: structural types for window.Translator (not in lib.dom), browserTranslateSupport(target) → unsupported/unavailable/available via Translator.availability({sourceLanguage:'en',targetLanguage}) with try/catch (throwing impl = unsupported), translateFlatInBrowser(flat, target) → per-string sequential translate(), null on any mid-way failure (no mixed-language catalogs — honest marker beats en+xx hybrid), empty model output keeps source value. Wired in context.tsx catch: server 400 → browser fallback attempt → success = setCachedCatalog (same sourceHash, IndexedDB cache works unchanged, one-time cost) + applyFlat + persist + title; failure/null = catalogUnavailable marker as before. Chrome pair list covers 29/36 of our langs (incl. uk,de,fr,es,pl,tr,ar,ja,ko,zh,vi,th,he,id,hi,bn); be/kk/uz/hy/ka/az/tg/fa/ur/sw keep the honest marker until a server translator exists. Mini-app untouched on purpose: TG webviews have no built-in Translator API (server path / marker only). Tests clientTranslate.test.ts (node env, ctor injected via globalThis.window): support classification ×4, per-string translation, null on missing API/unavailable pair, null on mid-way failure, null on create() failure, empty-output passthrough. Verified: tsc -b clean, vitest 54/54 (45 + 9), vite build ok. Free server-side alternative already supported (not built): TRANSLATOR_BASE_URL → llama.cpp/Ollama with Hy-MT1.5-1.8B (OpenAI-compatible, TRANSLATOR_API_KEY empty → 'not-needed'), documented in .env.example.
files: apps/browser/src/core/i18n/clientTranslate.ts, apps/browser/src/core/i18n/clientTranslate.test.ts, apps/browser/src/core/i18n/context.tsx, PROGRESS.md

## 2026-08-30  agent=human  id=i18n-local-mt
status: done
branch: main
notes: Free local specialized MT for dynamic UI languages: Hunyuan-MT-7B (WMT25 winner, 33 langs) — GGUF Q4_K_M (mradermacher) 4.6GB via Ollama, CPU-only, no API key. No Hunyuan-MT smaller than 7B exists (HF check; old "Hy-MT1.5-1.8B" default was fictional — .env.example now documents the real pull). speech.py: dedicated translator now speaks the model's exact training template — user-only messages (no system prompt), per-language English names (23/34 of our dynamic UI langs in the official table; be, uz, hy, ka, az, tg, sk, hu, ro, bg, el, sv, da, fi, no, sw absent → base-LLM best effort), zh target → Chinese ZH<=>XX variant, temperature 0.7 per model card (greedy meta-explains short ambiguous words — observed live), per-instance OpenAI client reuse (per-value loop), TRANSLATOR_TIMEOUT setting (default 60s, was hard-coded 30s). i18n_ui.py: when TRANSLATOR_BASE_URL is set the catalog translates per-value (JSON blob breaks specialized MT models and the max_tokens budget); translator down → honest 400 (client falls back to on-device Chrome Translator where available), meta-explained output (> 3×len+40) → one resample → source kept (one weird word must not kill a language). i18n.py: CATALOG_TTL 24h→7d (key already hashes content; amortizes slow CPU translation). Live box: ollama pull hf.co/mradermacher/Hunyuan-MT-7B-GGUF:Q4_K_M + local alias hunyuan-mt (num_ctx 2048 to cut KV RAM), .env TRANSLATOR_MODEL=hunyuan-mt. Shell smoke (de+kk, 4 real strings incl. {count}): natural de, placeholders preserved; kk correct but Arabic-script (Hunyuan's kk = China variant; a "Kazakh (Cyrillic script)" hint breaks the template → meta-explains in mixed Serbian — reverted to strict template). Perf: ~3-10s/string on loaded 8-core CPU, parallel requests thrash swap (ollama effectively serial per KV slot) → sequential per-value; full 443-key catalog = tens of minutes once per language per catalog version (server cache 7d + client IndexedDB persist). Tests: test_speech +2 (MT template user-only + zh variant; fake captures create-kwargs), test_i18n_api +3 (dedicated per-value, stub→400, garbage→source kept); JSON-path tests pin settings.TRANSLATOR_BASE_URL="" (pytest inherits root .env where it is now set). Verified: targeted 9/9; full backend pytest 203 passed (was 198); daphne :8005 restarted with new code/env, health ok (db/redis/channels). Mini-app/browser untouched.
files: backend/apps/dialogue/speech.py, backend/apps/common/i18n_ui.py, backend/api/v1/i18n.py, backend/config/settings/base.py, backend/apps/dialogue/tests/test_speech.py, backend/apps/common/tests/test_i18n_api.py, backend/apps/common/tests/test_i18n_ui.py, .env.example, PROGRESS.md

## 2026-08-30  agent=human  id=privacy-audit
status: done
branch: main
notes: PII storage audit behind the "no personal data" grant claim, then two hardening fixes. Inventory (verified in code): identity is optional (Account email/password/pseudonym/telegram_id — opt-in; AnonymousSession UUID+pseudonym+optional magic-link contact_email); content (stories/dialogues/messages/voice/circles) links to pseudonymous UUIDs only; Notification payload = id refs, no emails; EmailDigest.to_email only for opt-in soft-notify; no IP/user-agent anywhere in app code (the single REMOTE_ADDR use was the i18n rate-limit fallback); identity middleware only mints a cookie, logs nothing; no analytics/trackers/public like-counters in the data model; ZK patterns are device-only (IndexedDB). Fixes: (1) i18n.py _rate_key — a raw IP is never stored even transiently in Redis: SECRET_KEY-salted SHA-256 hash, same 20/3600s limit; (2) deploy/nginx-de-press.conf — log_format privacy (no $remote_addr, no $http_user_agent) + access_log in the 443 server (assets/static/media were already off). New PRIVACY.md (repo root): stored-data table with file refs, never-stored list, retention (voice delete_on_close default, circles purge on close), verifiability/self-host note — doubles as the grant artifact. Publish-prep fact: git history has zero secrets ever committed (only .env.example/.env.prod.example with empty values), .git = 9.7M. Verified: targeted i18n 3/3; full backend pytest 203 passed (92.8s).
files: backend/api/v1/i18n.py, deploy/nginx-de-press.conf, PRIVACY.md, PROGRESS.md

## 2026-08-30  agent=human  id=fund-contour
status: done
branch: main
notes: Non-custodial Solana fund contour (ADR-0020, browser SPA only — Mini App is a next track). Backend: settings TREASURY_SOLANA_ADDRESS/TREASURY_SQUADS_URL (empty = fund UI hidden) + FUND_STALE_GAP_MINUTES=15/FUND_MAX_MINUTES_PER_DAY=600; Account.tip_wallet_address (migration 0011, base58 32–44, ≠ treasury); new apps/fund — DutySegment ledger (open on duty-on via set_helper_duty hook, manual close credited to last heartbeat, lazy stale-close at last heartbeat, heartbeat-gap split in touch_helper hook using prev_seen BEFORE helper_seen_at update), duty_report with per-helper per-UTC-day cap and pseudonymous rows (no-pseudonym helpers → helper-<id[:8]>, email prefix never leaks to the public report; report reconciles stale open segments); api/v1/fund.py: public GET /v1/fund/info + GET /v1/fund/report?period=YYYY-MM, helper-only POST /me/tip-wallet (set/clear); MeOut += tip_wallet_address; router registered in api/main.py. DialogueOut += peer_tip_wallet — exposed only when dialogue is CLOSED and the peer is a helper with an opt-in wallet, shown to the grateful side only (helper's own view stays empty). Browser: features/fund — FundCard (treasury block: copy + solana:<addr>?spl-token=<USDC> deep-link + Squads/Solscan links, hidden while unset), TipWalletForm (MorePane, helper-only, deanonymization warning "a separate wallet"), TipBanner (thank-you card in DialoguePage after close); api client fundInfo/setTipWallet; Me/Dialogue types += tip fields; i18n fund.* ru/en/types. i18n catalog cap 500→560 (apps/common/i18n_ui.py MAX_KEYS, flatten.test.ts synced — catalog was already 495/499 before the feature). Tests: apps/fund 15 passed (segment lifecycle, stale/gap split, report math+cap+month filter, wallet validation, HTTP contract), dialogue test_tip_wallet.py 3 passed (hidden until closed, opt-in/clear, reopen hides), identity/help/presence suites green; full backend pytest re-run after MeOut change; browser vitest 57/57, tsc -b, vite build ok. Treasury value comes later from the user (after Squads Multisig creation) — feature degrades to a hidden block until set.
files: backend/config/settings/base.py, .env.example, backend/apps/identity/models.py, backend/apps/identity/migrations/0011_account_tip_wallet_address.py (new), backend/apps/identity/services.py, backend/apps/dialogue/presence.py, backend/apps/fund/ (new: __init__, apps, models, services, admin, migrations/0001_initial, tests), backend/api/v1/fund.py (new), backend/api/main.py, backend/api/v1/dialogue.py, backend/api/v1/identity.py, backend/apps/common/i18n_ui.py, apps/browser/src/core/api/types.ts, apps/browser/src/core/api/client.ts, apps/browser/src/features/fund/ (new: wallet.ts, wallet.test.ts, fund.module.css, FundCard.tsx, TipWalletForm.tsx, TipBanner.tsx), apps/browser/src/features/more/MorePane.tsx, apps/browser/src/features/chat/DialoguePage.tsx, apps/browser/src/core/i18n/types.ts, apps/browser/src/core/i18n/messages/ru.ts, apps/browser/src/core/i18n/messages/en.ts, apps/browser/src/core/i18n/flatten.test.ts, de-press-docs/app/adr/0020-non-custodial-fund.md (new), de-press-docs/app/ROADMAP.md, PRIVACY.md, PROGRESS.md
api: GET /api/v1/fund/info → {treasury_address, squads_url} | GET /api/v1/fund/report?period=YYYY-MM → {period, total_minutes, rows: [{pseudonym, minutes, share_percent}], treasury_address} | POST /api/v1/me/tip-wallet {address} → {ok, tip_wallet_address} | 403 non-helper | 400 invalid base58/treasury | DialogueOut += peer_tip_wallet

## 2026-08-30  agent=human  id=therapy-seed
status: done
branch: main
notes: Therapist contour seed per VOICE_THERAPY_PLAN stage B (ADR-0022). Backend apps/therapy: TherapistProfile (one-time admin invite token, admin auto-generates on create), TherapySession status machine awaiting_payment → payment_claimed → paid → done (+ declined by therapist), services claim_invite/create_session/mark_i_paid (anon clients allowed)/confirm_payment (idempotent, creates Dialogue with DialogueSource.THERAPY)/decline/complete, client+therapist listings with pseudonymous client_label. API v1: profiles (public, active only), claim, sessions POST, me/sessions, me/inbox, i-paid, confirm, decline, complete, detail; MeOut.is_therapist; apps.therapy in INSTALLED_APPS. Frontend features/therapy: TherapyPane (catalog + request, both cabinets, therapist invite claim; route /therapy, MorePane row, lazy chunk), PayModal — Solana Pay QR (solana:<addr>?amount=… deep-link via `qrcode` deps, pattern of ADR-0020 FundCard) + copy + i-paid + awaiting state; SessionOut exposes therapist's public solana_address for the QR. i18n therapy.* (30 keys ru/en/types) + nav.therapy + common.close; catalog cap 560 → 640 (i18n_ui.py MAX_KEYS, flatten.test.ts synced). Tests: apps/therapy 7 passed; full suite green in chunks (backend 233: therapy 7, fund 15, identity 21, common+notifications 48, dialogue 74, ai 13, empathy+moderation 7, support 22, stories 26) — note: run AI/support chunks with `env -u DEEPSEEK_API_KEY`, otherwise offline-stub tests fail against the live key configured on this box (env artifact, not a regression); browser vitest 64/64, tsc -b, vite build ok. Docs: ADR-0022 (new), PRIVACY.md (therapy rows + no-processing note), ROADMAP already had the seed section from commit 098e0c6.
files: backend/apps/therapy/ (new: models, services, admin, apps, migrations, tests), backend/api/v1/therapy.py (new), backend/api/main.py, backend/api/v1/identity.py, backend/apps/dialogue/models.py, backend/apps/dialogue/migrations/0013_alter_dialogue_source.py, backend/config/settings/base.py, backend/apps/common/i18n_ui.py, apps/browser/package.json, apps/browser/package-lock.json, apps/browser/src/App.tsx, apps/browser/src/features/more/MorePane.tsx, apps/browser/src/features/therapy/ (new: TherapyPane.tsx, TherapyPane.module.css), apps/browser/src/core/api/{types.ts,client.ts}, apps/browser/src/core/i18n/{types.ts,messages/ru.ts,messages/en.ts,flatten.test.ts}, de-press-docs/app/adr/0022-therapist-contour.md (new), PRIVACY.md, PROGRESS.md
api: GET /api/v1/therapy/profiles (public) | POST /api/v1/therapy/claim {invite_token} | POST /api/v1/therapy/sessions {therapist_id, note?} | GET /api/v1/me/therapy/sessions | GET /api/v1/me/therapy/inbox (therapist) | POST /api/v1/therapy/sessions/{sid}/i-paid | …/confirm | …/decline | …/complete | GET /api/v1/therapy/sessions/{sid} | SessionOut += solana_address | MeOut.is_therapist

## 2026-08-30  agent=human  id=miniapp-therapy-parity
status: done
branch: main
notes: Mini App therapy parity (ADR-0022 tail, PARITY_QA_PLAN P1.1). features/therapy/{TherapyPane.tsx,TherapyPane.module.css} ported from browser 1:1 (static import per mini-app convention; PayModal uses t.therapy.close — mini-app common.close means "close dialogue", browser's generic "Close" didn't fit). api client: 9 therapy endpoints + types (TherapistProfileOut/TherapySession/TherapySessionStatus; Me.is_therapist — backend already returns it via identity.py MeOut). i18n: therapy.* 31 keys + nav.therapy (ru/en/types). Sidebar rail: NavKey "therapy" (navOrder union + DEFAULT_NAV_ORDER + isNavKey — merge logic adds the key to existing local prefs automatically, no storage bump), heartbeat icon, /therapy route in App.tsx. Deps: qrcode ^1.5.4 + @types/qrcode ^1.5.5 (same as browser). Verified: npm run build (tsc -b + vite build) ✓ 7.9s; catalog ~490 keys < 640 cap. Fund parity = P1.2 (next), calls decision = Q2. Product questions of the new phase collected in OPEN_QUESTIONS.md (voice/video policy recommendation: status-quo — voice/video live in the dialogue, not in the role; helper dashboard = helper-only left tab evolving /helper; repo prep + EN docs + full retest plan in PARITY_QA_PLAN.md, ROADMAP priorities updated).
files: apps/mini-app/src/features/therapy/ (new), apps/mini-app/src/core/api/{client.ts,types.ts}, apps/mini-app/src/core/i18n/{types.ts,messages/ru.ts,messages/en.ts}, apps/mini-app/src/core/navOrder.ts, apps/mini-app/src/components/layout/{NavIcons.tsx,Sidebar.tsx}, apps/mini-app/src/App.tsx, apps/mini-app/package.json, apps/mini-app/package-lock.json, de-press-docs/app/PARITY_QA_PLAN.md (new), de-press-docs/app/OPEN_QUESTIONS.md (new), de-press-docs/app/ROADMAP.md, PROGRESS.md

## 2026-08-30  agent=human  id=phase-decisions
status: done
branch: main
notes: All 10 phase questions answered by the user and recorded (ROADMAP "Phase decisions" table, OPEN_QUESTIONS statuses, PARITY_QA_PLAN synced). Q1 voice/video = A, status-quo (in-dialogue, role-agnostic — confirmed). Q2 = port calls to Mini App (P1.3 confirmed work, not deferred). Q3 = A, helper-only left tab evolving /helper. Q4 = WS queue channel from the start (no polling). Q5 = GitHub publication is the LAST task of the whole project (license/CI/README at that moment; roadmap priorities table now has the "after everything" row). Q6 = A, EN canon; RU docs to be copied into docs-ru-archive/ at repo root before translation, folder added to .gitignore now. Q7 = no direct calls to helper. Q8 = ethical metrics accepted. Q9 = A, manual checklist retest. Q10 = anon therapy clients stay; recorded as privacy invariants: anon has the same capabilities as registered users; ALL history ALWAYS lives in the user's browser, never on the server (added to ROADMAP "Not doing"). Plan changes from decisions: P1.3 calls port is in scope after P1.2 fund; P2 repo-prep section re-scoped to publication-at-the-very-end; P5 dashboard queue via WS (notifications-WS pattern).
files: de-press-docs/app/ROADMAP.md, de-press-docs/app/OPEN_QUESTIONS.md, de-press-docs/app/PARITY_QA_PLAN.md, .gitignore, PROGRESS.md

## 2026-08-30  agent=human  id=miniapp-fund-parity
status: done
branch: main
notes: Mini App fund parity (ADR-0020 tail, PARITY_QA_PLAN P1.2). features/fund/{FundCard,TipBanner,TipWalletForm}.tsx + wallet.ts + fund.module.css ported from browser 1:1 (same @/ aliases; common.save/common.error already exist in mini catalog). Mounted: FundCard + helper-only TipWalletForm in UserMenu (mini-app's MorePane equivalent), TipBanner in DialoguePage above composer — renders only when peer_tip_wallet is exposed. api client: fundInfo + setTipWallet; types: FundInfo/TipWalletResponse, Me.tip_wallet_address, Dialogue.peer_tip_wallet. Backend checked, no change needed: /me/dialogues uses the same DialogueOut serializer that computes peer_tip_wallet (closed dialogue + grateful side + helper opt-in only). i18n: fund.* 15 keys (ru/en/types synced), catalog 492 keys < 640 cap. Wallet test decision: vitest stays browser-only (apps/browser/src/features/fund/wallet.test.ts covers the identical ported logic; no vitest dep added to mini-app). Verified: npm run build (tsc -b + vite build) ✓ 1.94s. Next: P1.3 calls in Mini App (Q2).
files: apps/mini-app/src/features/fund/ (new), apps/mini-app/src/features/chat/DialoguePage.tsx, apps/mini-app/src/components/layout/UserMenu.tsx, apps/mini-app/src/core/api/{client.ts,types.ts}, apps/mini-app/src/core/i18n/{types.ts,messages/ru.ts,messages/en.ts}, de-press-docs/app/PARITY_QA_PLAN.md, de-press-docs/app/ROADMAP.md, PROGRESS.md

## 2026-08-30  agent=human  id=miniapp-calls-parity
status: done
branch: main
notes: Mini App calls parity (ADR-0021 tail, PARITY_QA_PLAN P1.3, decision Q2). features/calls/{callMachine.ts,useCall.ts,CallModal.tsx,CallModal.module.css} ported from browser 1:1 (callMachine.test.ts stays browser-only — same vitest decision as wallet). Signaling via the existing dialogue WS: useChatSocket gained CallSignalEvent/ChatSocketOptions (optsRef), a `call.*` forwarding branch in handleEnvelope and fire-and-forget sendCall — mirrors the browser hook exactly. api client: rtcConfig (GET /api/v1/rtc/config, ICE servers; backend unchanged). DialoguePage: useCall wired before useChatSocket ({ onCall: call.onSignal }), sendCallRef, onSocketDown effect on wsStatus, 📞 header button (disabled while dialogue not open, hidden entirely when navigator.mediaDevices is absent — feature-detect per plan), CallModal overlay at page end. i18n: calls.* 17 keys (ru/en/types synced), catalog 509 keys < 640 cap. Verified: npm run build (tsc -b + vite build) ✓ 2.42s. P1 browser↔Mini App parity is now complete (P1.1 therapy, P1.2 fund, P1.3 calls). Next per plan: P3 EN docs translation, P4 full retest, P5 helper dashboard, P2 publication last (Q5).
files: apps/mini-app/src/features/calls/ (new), apps/mini-app/src/features/chat/{DialoguePage.tsx,DialoguePage.module.css}, apps/mini-app/src/core/hooks/useChatSocket.ts, apps/mini-app/src/core/api/client.ts, apps/mini-app/src/core/i18n/{types.ts,messages/ru.ts,messages/en.ts}, de-press-docs/app/PARITY_QA_PLAN.md, PROGRESS.md

## 2026-08-30  agent=human  id=p3-en-docs-translation
status: done
branch: main
notes: P3 EN docs translation (Q6: EN canon). Step 1: all RU md files archived to docs-ru-archive/ (root/, app/, app/adr/ — 45 files, gitignored, untracked by design) BEFORE any edit. Step 2: GLOSSARY.md (new, de-press-docs/app) — ~30 canonical RU→EN term pairs (helper, support cloud, quiet phrases, video circles, treasury, tip wallet, duty, hearer, outreach...). Step 3: full translation of the root batch (README, PRIVACY, CONTEXT, MAINPLAN, PROGRESS, CLAUDE) — MAINPLAN/README/PRIVACY fully rewritten in EN, CONTEXT had 3 RU inserts, PROGRESS converted via targeted sed over the notes lines. Step 4: app docs — full translation of ROADMAP, OPEN_QUESTIONS, PARITY_QA_PLAN, PLATFORMS, ANTI_PANIC, app README, VOICE_THERAPY_PLAN, DESIGN_V2 (incl. the ASCII 3-zone scheme relabeled), FRONTEND_PLAN (incl. css-token comments), DEPLOY; ADR 0021/0022 fully; ADR 0004/0018/0020 + PILOT/TG_SHELL_SPEC/MINI_APP/MOBILE_PWA/FEED_VOICE/TTI_BASELINE — a fragment sweep. Deliberately kept RU: GLOSSARY RU column (its purpose), code/legacy quotes (msg.includes("одно"), the legacy "[офлайн…]" sentinel, the canonical "Мне хуево, отвали"/"Я слышу тебя" UI phrases — now with EN glosses). Verification: repo-wide grep — 0 Cyrillic in all md except the 5 files above (each an intended exception); content/structure/links preserved line-by-line where feasible. Not translated: de-press-docs/site/ (not in the P3 inventory), docs-ru-archive (the archive itself).
files: README.md, PRIVACY.md, CONTEXT.md, MAINPLAN.md, PROGRESS.md, CLAUDE.md, de-press-docs/app/GLOSSARY.md (new), de-press-docs/app/{ROADMAP,OPEN_QUESTIONS,PARITY_QA_PLAN,PLATFORMS,ANTI_PANIC,README,VOICE_THERAPY_PLAN,DESIGN_V2,FRONTEND_PLAN,DEPLOY,PILOT,TG_SHELL_SPEC,MINI_APP,MOBILE_PWA,FEED_VOICE,TTI_BASELINE}.md, de-press-docs/app/adr/{0004,0018,0020,0021,0022}*.md, docs-ru-archive/ (gitignored)

## 2026-08-30  agent=human  id=p4-automated-retest
status: done
branch: main
notes: P4 full retest — the automated part (per PARITY_QA_PLAN; the runner 30s timeout forces chunking). Backend pytest (DEPRESS_USE_SQLITE=1 CHANNEL_LAYER=memory), chunks and results: therapy+fund 22, identity 21, notifications+common 48, empathy+moderation 7, support 22, stories 26 (api 7 + services/realtime 19), ai 13 (env -u DEEPSEEK_API_KEY — the env artifact noted in the therapy entry), dialogue 74 (speech+services 26, calls+tip_wallet 10 — the new P1.2/P1.3 server surface, help_api+presence 13, help 16, realtime+api_chat_prefs 6, dialogue_review+api_circle 3). Total 233 passed = the historical full-suite count, no regressions after the fund/calls ports. Frontend: browser vitest 64/64 (11 files), tsc -b + vite build ✓ 5.39s; mini-app tsc -b + vite build ✓ 5.82s. Remaining for P4: the manual smoke matrix (P6) — needs live devices/Telegram, owner = human.
files: PROGRESS.md (no code changes — verification only)

## 2026-08-30  agent=human  id=p5-helper-dashboard
status: done
branch: main
notes: P5 Helper dashboard (browser, per ROADMAP Q3/Q4/Q8). Backend: `apps/dialogue/queue_realtime.py` (shared `helpers.queue` group + broadcast), broadcasts wired into create/accept/cancel help flows, `HelperQueueConsumer` at `WS /ws/helper/queue/` (Helpers only, 4403 otherwise; snapshot = the same view as the HTTP inbox — skips/own excluded, empty while off duty; client→`queue.refresh` re-pull for duty toggles; events `queue.new/taken/cancelled`), Q8 metrics endpoint `GET /api/v1/help/dashboard` (helper/staff; queue length, median wait 7d, taken/closed 24h+7d, on-duty + online-of-those — no per-helper seconds/ratings). Frontend: `useHelperQueue` hook (useNotifications WS pattern, no polling), `/helper` evolves: new default tab "Requests" with metrics strip + live queue rows (wait ticking, note) + Take → accept → `/chat/:id`, off-duty hint, duty toggle now refreshes queue + metrics. i18n helper.* +17 keys (ru/en/types). Backend tests new `test_helper_queue.py` (WS: auth rejection, snapshot+live new/taken+refresh, cancel; API: metrics lifecycle, 403 anon/403 non-helper/200 staff) — DEPRESS_USE_SQLITE chunks: helper_queue+realtime+notif-consumers 10, help 16, help_api 5, presence+tip_wallet 8 = 39 passed, no regressions. Browser: tsc -b ✓, vitest 64/64, vite build ✓ 3.96s. Mini App untouched (P5 is a browser tab by plan).
files: backend/apps/dialogue/{queue_realtime.py,consumers.py,help.py,routing.py,tests/test_helper_queue.py}, backend/api/v1/help.py, apps/browser/src/features/helper/{useHelperQueue.ts,HelperQueue.tsx}, apps/browser/src/core/api/{client.ts,types.ts}, apps/browser/src/core/i18n/{types.ts,messages/ru.ts,messages/en.ts}, PROGRESS.md

## 2026-08-31  agent=human  id=plan-sync
status: done
branch: main
notes: Plan hygiene after P5 — ROADMAP: the three P0 rows (parity tails, helper dashboard, docs+retest) and the P1 Telegram Mini App host row marked ✅ (initData auth + theme bridge + soft-notify are in the code with tests; the bot token/webhook — deploy-time config), the stale "next: native dynamic multilingual" stage line replaced with the pilot-readiness line (the drop was already fixed in the Voice section and the Next table). PARITY_QA_PLAN: the P4 automated part marked done, P5 ✅ done. What remains per the plan: P6 manual QA (live devices/Telegram — human), pilot ops / media-S3 / secrets-backup (credentials — human), the own-desktop host decision, Geo-help v2 + pre-mod AI-assist for reports (P2, no spec yet), the fund phase-2 signature ownership verification (ADR-0020 tail), the AI assistant last, the publication after everything (Q5).
files: de-press-docs/app/ROADMAP.md, de-press-docs/app/PARITY_QA_PLAN.md, PROGRESS.md

## 2026-08-31  agent=human  id=fund-phase2
status: done
branch: main
notes: ADR-0020 phase 2 — off-chain ed25519 proof of tip-wallet ownership, surfaced as a "verified" badge. Backend: Account.tip_wallet_verified_at + migration 0012; apps/fund/services.py — canonical _CHALLENGE + challenge_message(address, nonce) (the nonce travels with the signature, so the backend rebuilds the exact signed bytes with no stored state — fixes the earlier regenerate-nonce bug), dependency-free _b58decode, verify_tip_wallet_signature (64-byte sig, local ed25519 verify, no keys/tx/RPC per ADR); api/v1/fund.py — TipWalletIn.{signature,nonce}, TipWalletOut.tip_wallet_verified; semantics: empty address clears, same address w/o signature keeps verified_at, a different address w/o signature clears it, a bad signature = 400 and changes nothing, a signature without its nonce = 400; /me and DialogueOut expose tip_wallet_verified / peer_tip_wallet_verified. Browser FE: wallet.ts — challengeMessage (byte-identical contract test vs the backend template), dependency-free b58encode, signTipWalletChallenge over the injected wallet (window.solana / phantom.solana — a message approval, never a transaction), TipWalletForm signature branch (no wallet / user decline → falls back to saving unverified), the verified badge on the form and on TipBanner, api types + client payload, i18n fund.verifiedBadge (ru/en/types), .badge CSS. Mini App: the same port 1:1 (a Telegram host without an injected wallet degrades to unverified — documented in wallet.ts). Tests: backend fund 19/19 + identity 21/21 + dialogue 71/71 (DEPRESS_USE_SQLITE chunks, EXIT=0); browser vitest 69/69 (wallet.test.ts +7), tsc -b ✓; mini-app tsc -b + vite build ✓ (vitest stays browser-only by decision). cryptography>=42 added to backend/requirements/base.txt (was transitive only). Pre-existing, untouched: makemigrations --check proposes a support-app index rename (0009) — unrelated drift.
files: backend/apps/identity/{models.py,migrations/0012_account_tip_wallet_verified_at.py}, backend/apps/fund/{services.py,tests/test_duty_fund.py}, backend/api/v1/{fund.py,identity.py,dialogue.py}, backend/requirements/base.txt, apps/browser/src/features/fund/{wallet.ts,wallet.test.ts,TipWalletForm.tsx,TipBanner.tsx,fund.module.css}, apps/browser/src/core/api/{client.ts,types.ts}, apps/browser/src/core/i18n/{types.ts,messages/en.ts,messages/ru.ts}, apps/browser/src/features/{more/MorePane.tsx,chat/DialoguePage.tsx}, apps/mini-app/src/features/fund/{wallet.ts,TipWalletForm.tsx,TipBanner.tsx,fund.module.css}, apps/mini-app/src/core/api/{client.ts,types.ts}, apps/mini-app/src/core/i18n/{types.ts,messages/en.ts,messages/ru.ts}, apps/mini-app/src/components/layout/UserMenu.tsx, apps/mini-app/src/features/chat/DialoguePage.tsx, de-press-docs/app/ROADMAP.md, PROGRESS.md

## 2026-08-31  agent=human  id=admin-plan
status: done
branch: main
notes: The pre-launch pivot — the admin dashboard (moderation + reports). What exists (checked): Report (story|message, reasons abuse/spam/self_harm/other, statuses open/reviewing/resolved_*), the submit endpoints, the helper aggregate (pending_clouds, open/reviewing, recent_reports), Story.status hidden/removed, notifications.notify, no analytics anywhere (the visitor counts will come from the identity.Visitor creations — counts only). What is decided (Q11/Q12): a separate Vite app apps/admin (several future admins; the admin code never ships to the regular users); the aggregates only — the visitors per 24h/7d (no IPs/fingerprints/lists), the public stories + hear counts, the dialogues opened/closed as counts, the therapy funnel by status, the help metrics (Q8), the reports by status/reason; the moderation sees only the reported item (no arbitrary browsing, the identities are not exposed), the removal reason is mandatory + the ModerationAction audit log, the default is a reversible hide (Story.status hidden), the removal is exceptional; the reporter gets a notification without the other party's data; the helpers get the view-only reports panel in /helper. The plan: A1 the admin app skeleton (Vite+React+ReactQuery, the staff session auth, 403), A2 the backend moderator surface (GET /admin/overview, GET /admin/reports, POST /admin/reports/{id}/resolve, the ModerationAction model, the tests), A3 the admin UI (Overview/Reports/Log, admin.* i18n en/ru), A4 the helper reports panel, A5 the version bump + the P6 matrix extension + the DEPLOY.md note (/admin hosting). The versions: the apps 0.1.0 → 1.0.0 (the ladder: 1.0 the pre-launch line, 1.1 the AI assistant, then the publication Q5, 1.3 own mobile, 1.5 own desktop — approximate); the PWA polish is frozen until after the pilot; Own Mobile/Desktop moved to after the launch. Out of scope (P2 unchanged): the pre-mod/AI-assist for the reports, Geo-help v2.
files: de-press-docs/app/{ROADMAP.md,PARITY_QA_PLAN.md,OPEN_QUESTIONS.md}, apps/browser/package.json, apps/mini-app/package.json, PROGRESS.md

## 2026-08-31  agent=human  id=admin-dashboard
status: done
branch: main
notes: A1–A5 admin dashboard implemented. Owner decision this session: the AI assistant and Geo-help v2 move AFTER the launch (ROADMAP stage line + Next table + version ladder now 1.0 launch line = admin+moderation, pilot, publication Q5; 1.1 AI assistant; 1.2 Geo-help v2 + pre-mod AI-assist; 1.3 mobile; 1.5 desktop; PARITY_QA_PLAN P7 out-of-scope note updated). Backend (A2): ModerationAction model (audit log, migration 0005; report/story/message/actor FKs, action reviewing|hidden|removed|dismissed, reason mandatory for terminal decisions, note) — resolve_report rewritten: decision API (hide|remove|dismiss|reviewing), reason required for terminal decisions, sibling open reports on the same target auto-resolved with the same decision, reporters notified (new NotificationKind report_resolved — payload only report_id/decision, no other-party data); story hide → moderate_story(hidden) with the existing WS events, remove → removed; message hide/remove → new dialogue.services.moderator_scrub_message (the same _scrub_message path as delete-for-everyone + message.deleted WS broadcast, pinned reset); Django admin actions migrated to the new signature (reason=other defaults). Staff API in api/v1/moderation.py: GET /admin/overview (counts only: sessions 24h/7d/total from AnonymousSession creations, public stories total/7d, hears, dialogues open/closed, therapy_by_status, pending_clouds, reports open/reviewing/7d/by_reason — no content, no identities), GET /admin/reports?status=&limit= (target_kind story|message, target_text = public story body / 600-char message preview, target_hidden, NO reporter identity fields), POST /admin/reports/{id}/resolve (403 non-staff, 400 invalid decision/reason/already-resolved, 404 unknown), GET /admin/moderation-log (actor email, action, reason, note). Tests apps/moderation/tests/test_admin.py: 11 passed (permissions anon/user/helper/staff, overview counts-only privacy, reports queue privacy, hide/remove/dismiss flows, sibling auto-resolve, notifications, message scrub, reason-mandatory, double-resolve, 404) — full moderation suite 15 passed. Admin app (A1+A3): apps/admin — Vite+React+ReactQuery, session-cookie api client (401/403 → access-denied screen with a link to the main app), tabs Overview (aggregate cards + reports-by-reason + therapy statuses) / Reports (status filter, resolve form: decision hide(dismiss)(remove), reason select, optional note) / ModerationLog (table), i18n admin.* en/ru (explicit Messages interface), light calm CSS; vite base=/console/, port 5175, /api proxy. Hosting (A5): nginx location /console/ (alias admin/dist, SPA fallback, X-Robots-Tag noindex) — /admin stays the Django admin fallback; DEPLOY.md scheme + build/rsync steps + a note that /api/v1/admin/* is staff-session-only. A4 helper reports panel: already shipped as the /helper Summary tab (metrics + recent_reports view-only) — no changes needed. Browser/mini: NotificationKind + report_resolved kind label (en/ru/i18n types) so the reporter notification renders.
files: backend/apps/moderation/{models.py,migrations/0005_moderationaction.py,services.py,dashboard.py,admin.py,tests/{test_services.py,test_admin.py}}, backend/apps/dialogue/services.py, backend/apps/notifications/models.py, backend/api/v1/moderation.py, apps/admin/**, deploy/nginx-de-press.conf, de-press-docs/app/{DEPLOY.md,ROADMAP.md,PARITY_QA_PLAN.md}, apps/browser/src/core/{api/types.ts,i18n/{types.ts,messages/en.ts,messages/ru.ts}}, apps/mini-app/src/core/{api/types.ts,i18n/{types.ts,messages/en.ts,messages/ru.ts}}, PROGRESS.md
api: GET /api/v1/admin/overview
api: GET /api/v1/admin/reports?status=open|reviewing|resolved_hidden|resolved_dismissed|all
api: POST /api/v1/admin/reports/{report_id}/resolve {decision: hide|remove|dismiss, reason, note}
api: GET /api/v1/admin/moderation-log

## 2026-08-31  agent=human  id=migration-drift
status: done
branch: main
notes: Closed the makemigrations --check drift: support/0009 (RenameIndex support_sup_thread__idx → support_sup_thread__c93351_idx — the pre-existing model/migrations mismatch) + notifications/0006 (AlterField kind — the no-op schema sync for the report_resolved choice added with the admin dashboard; choices changes still produce an AlterField). --check is clean now; migrate applied cleanly to the local Postgres dev DB (also picked up moderation.0005 and therapy.0001 it was missing). Chunk support+moderation+notifications: 55 passed (DEPRESS_USE_SQLITE). Fund public report page moved out of the app scope — the owner will show it on the promo/landing site (the ROADMAP ADR-0020 section updated; the API GET /v1/fund/report stays for the landing to consume).
files: backend/apps/support/migrations/0009_rename_support_sup_thread__idx_support_sup_thread__c93351_idx.py, backend/apps/notifications/migrations/0006_alter_notification_kind.py, de-press-docs/app/ROADMAP.md, PROGRESS.md

## 2026-08-31  agent=human  id=blocked-modal-voice-hold
status: done
branch: main
notes: Chat parity batch. (1) Blocked-users management: GET /api/v1/blocks (the viewer's Block rows → BlockItemOut {id, created_at, label, target_kind}; labels via Account/AnonymousSession display_pseudonym, select_related, newest first) + DELETE /api/v1/blocks/{block_id} (unblock_by_id deletes only rows where the viewer is the blocker; 400 on no-identity, ok=false when nothing deleted); apps/moderation/blocks.py list_blocks_for + BlockedEntry; test_list_blocks_for_and_unblock_by_id (both target kinds, pseudonym labels, cross-actor isolation, unknown id). Browser + mini ChatList: ⊘ button next to the search (searchWrap → flex, .blockedBtn pill), BlockedUsersModal (react-query list + per-row Unblock mutating DELETE, invalidates blocks+dialogues, Esc/backdrop/✕ close, empty + error notes) with its own CSS; api client myBlocks/unblockById + BlockItem type; i18n chat.blockedTitle/blockedEmpty/unblock en/ru (+ mini common.close, it lacked it). (2) Voice = hold-to-record in both apps: mic onPointerDown starts recording, window pointerup/pointercancel stops → MediaRecorder onstop sends immediately via sendVoice (no preview/cancel step); voiceStartingRef guards double permission prompts, voiceReleasedRef covers release-during-permission; the recording mic gets .recBtn panic-red tint + title. Removed the "[голосовое сообщение]" body from voice bubbles: msgBody hidden when isVoice (backend body/duration untouched — dialogue test 212 stays green; audio bubble + meta render as before). Verify: backend moderation+dialogue 95 passed (DEPRESS_USE_SQLITE); browser tsc -b + vite build ✓, vitest 69/69; mini tsc -b + vite build ✓.
files: backend/apps/moderation/{blocks.py,tests/test_services.py}, backend/api/v1/moderation.py, apps/browser/src/features/chat/{BlockedUsersModal.tsx,BlockedUsersModal.module.css,ChatList.tsx,ChatList.module.css,DialoguePage.tsx,DialoguePage.module.css}, apps/browser/src/core/{api/{client.ts,types.ts},i18n/{types.ts,messages/en.ts,messages/ru.ts}}, apps/mini-app/src/features/chat/{BlockedUsersModal.tsx,BlockedUsersModal.module.css,ChatList.tsx,ChatList.module.css,DialoguePage.tsx,DialoguePage.module.css}, apps/mini-app/src/core/{api/{client.ts,types.ts},i18n/{types.ts,messages/en.ts,messages/ru.ts}}, PROGRESS.md
api: GET /api/v1/blocks
api: DELETE /api/v1/blocks/{block_id}

## 2026-08-31  agent=human  id=retest-refresh
status: done
branch: main
notes: P4 automated retest refresh before the manual QA (the code moved since the 08-30 pass: the admin dashboard, the tip-wallet verification, the blocks API + modal, hold-to-record). Backend chunks (DEPRESS_USE_SQLITE): therapy 7, fund 19, identity 21, common+notifications 48, dialogue 79, ai 13 (env -u DEEPSEEK_API_KEY), empathy+moderation 19, support 22, stories 26 — 254 passed / 0 failed; makemigrations --check clean (note: --check needs the system Postgres env — DEPRESS_USE_SQLITE is pytest-only and raises outside tests). Frontend: browser tsc -b + vite build ✓, vitest 69/69 (11 files); mini-app build ✓; admin build ✓ (its first retest — the app landed after 08-30). Runner notes: pytest.ini addopts already carries -q, an extra -q on the CLI degrades to -qq (dots, no summary line); the heavy chunks (dialogue ~85s, support/stories ~35s) exceed the ~30s runner timeout — launch via setsid into a log and poll. Next per the roadmap: the manual smoke matrix (P6, owner = human; add the ⊘ blocked-users modal, hold-to-record voice, the hidden voice placeholder to the checklist), then fixes + a re-run.
files: PROGRESS.md

## 2026-08-31  agent=human  id=drop-notifications-admin-en-help-shell
status: done
branch: main
notes: Three agreed changes. (1) Notifications tab removed from both apps: browser navOrder/Sidebar/TabBar (unread polling query + soft-badge logic dropped), /notifications route + lazy NotificationsPane import removed from App.tsx, NotificationsPane.{tsx,module.css} deleted, .bare full-bleed class removed from App.module.css; mini-app navOrder (+isNavKey)/Sidebar (unread query + badge), /notifications dropped from SECTION_ROOTS (host/navigation.ts), pane deleted, whole notifications i18n block removed from mini types/en/ru (browser keeps only notifications.openNotice + kind.* — Shell toasts still use them). Bot soft-notify deep links (notifications|notify|inbox|n) now land on /feed (host/startParam.ts) so old telegram links don't 404. Backend notification API endpoints and telegram_notify.py untouched (api client methods + useNotifications live hook remain, simply unused by nav); email inbox (InboxPage) unaffected. (2) Django admin anglicized + missing models registered: LANGUAGE_CODE="en-us" (base.py), English site_header/site_title/index_title ("Operations") in config/urls.py, view-only ModelAdmins: dialogue HelpRequest + HelpRequestSkip + MessageHide, moderation ModerationAction (audit log), notifications EmailDigest. (3) /help restores the nav Shell: HelpChrome full-bleed wrapper + main.bare deleted from browser App.tsx — /help, /help/wait, /help/ai render inside Shell like every other pane; unused imports (useViewportMode/ViewportMode/Outlet/AntiPanicOverlay) cleaned (AntiPanicOverlay stays mounted inside Shell). Verify: browser tsc -b + vite build ✓, vitest 69/69 (11 files); mini tsc -b + vite build ✓; backend manage.py check clean; full backend suite 252/254 — the 2 failures are apps/ai offline-stub tests (test_support_stream_sse_contract, test_stream_offline_single_chunk) failing only because the runner shell exports DEEPSEEK_API_KEY (not in the project .env): with `env -u DEEPSEEK_API_KEY` apps/ai is 13/13 passed (same caveat as the retest-refresh entry). Toast click-path verified post-removal (concern flagged during the session proved a false alarm): the only notification toast is browser Shell LiveNotifications → navigate(resolveTarget(n) ?? /chat); resolveTarget never emits /notifications — kinds map to /chat, /chat/{id}, /feed[/{id}?cloud=&entry=], /help/wait, all still routed; mini Shell runs useNotifications(!active) without an onNew callback (no toast, no navigation); zero literal /notifications paths left in either frontend.
files: apps/browser/src/{App.tsx,App.module.css,components/layout/{Sidebar.tsx,TabBar.tsx},core/{navOrder.ts,i18n/{types.ts,messages/{en.ts,ru.ts}}}}, apps/browser/src/features/notifications/NotificationsPane.{tsx,module.css} (deleted), apps/mini-app/src/{App.tsx,components/layout/Sidebar.tsx,core/{navOrder.ts,host/{navigation.ts,startParam.ts},i18n/{types.ts,messages/{en.ts,ru.ts}}}}, apps/mini-app/src/features/notifications/NotificationsPane.{tsx,module.css} (deleted), backend/{config/{settings/base.py,urls.py},apps/dialogue/admin.py,apps/moderation/admin.py,apps/notifications/admin.py}, PROGRESS.md

## 2026-08-31  agent=human  id=ponytail-cleanup
status: done
branch: main
notes: Ponytail-review dead-code cleanup on top of drop-notifications-admin-en-help-shell (net ≈ −430 lines, zero behavior change for users). (1) Browser useNotifications.ts 250→~140: removed the dead syncQuery/loadViaHttp HTTP path + its 45s poll (2 unread requests per open tab), the snapshot/unread_count/error envelope arms, status/error/reconnectAttempt state and the whole return object — the hook keeps WS connect/reconnect/backoff, registerSocket, live-key invalidations (dialogues, dialogue-requests, story, feed, my-stories, hearers, help-requests, help-mine) and the onNew toast callback (cloud kinds still filtered). (2) Mini useNotifications.ts deleted (−202): every cache write had no readers, it never invalidated feed/dialogues, no onNew — plus its LiveNotifications wrapper dropped from mini Shell; core/ws/{registry,url} stay (feed/chat/anti-panic use them). (3) Reader-less invalidate pairs removed: browser+mini StoryPage (cloud-read), browser InboxPage (kept ["me"], comment → "refresh who-am-i"). (4) Browser i18n notifications.kind → Partial<Record<NotificationKind, string>> keeping exactly the 6 CHAT_NOTICE_KINDS labels (dialogue_request, support_cloud, cloud_approved, dialogue_opened, outreach_intro, message; Shell falls back ?? n.kind), 6 unreachable ones deleted from types/en/ru (en/ru stay key-aligned for flatten.test; resolveTarget keeps mapping legacy kinds — untouched). (5) IconBell + "bell" removed from NavIcons in both apps. (6) client.ts both apps: zero-caller notifications/notificationsUnreadCount/markNotificationRead/markAllNotificationsRead deleted + names pruned from the import type block (export type {...} from "./types" re-export keeps the surface; noUnusedLocals clean). (7) Backend: new apps/common/admin.py ReadOnlyAdmin (add/change/delete → False); dialogue HelpRequest/HelpRequestSkip/MessageHide, moderation ModerationAction, notifications Notification/EmailDigest rebased onto it (duplicated permission methods deleted; the dialogue trio now also blocks delete — closes the staff-delete gap); no-op raw_id_fields dropped (ModerationAction — all FKs already readonly; EmailDigest) and duplicate created_at dropped from their list_filter (date_hierarchy covers it). Verify: browser tsc -b + vite build ✓, vitest 69/69 (11 files); mini tsc -b + vite build ✓; backend manage.py check clean; full suite 256 passed (DEPRESS_USE_SQLITE, env -u DEEPSEEK_API_KEY, 8:22); greps clean for "bell"/IconBell/NotificationsStatus/notifications-unread and the deleted kind labels (remaining unread_count is the Dialogue chat field; dialogue_request_review lives only in resolveTarget's kind→route map).
files: apps/browser/src/{core/{hooks/useNotifications.ts,api/client.ts,i18n/{types.ts,messages/{en.ts,ru.ts}}},components/layout/NavIcons.tsx,features/{feed/StoryPage.tsx,inbox/InboxPage.tsx}}, apps/mini-app/src/{components/layout/{Shell.tsx,NavIcons.tsx},core/{api/client.ts,hooks/useNotifications.ts (deleted)},features/feed/StoryPage.tsx}, backend/apps/{common/admin.py (new),dialogue/admin.py,moderation/admin.py,notifications/admin.py}, PROGRESS.md

## 2026-09-01  agent=human  id=bug-reports
status: done
branch: main
notes: Bug-report pipeline (see a problem → report it; staff reads it in admin). Backend: concrete BugReport in apps/common (TimeStamped+UUID bases; Status TextChoices new/done, default new; reporter_account/reporter_session SET_NULL string-FKs to identity — session only when no account, blocks.py convention; text + page + user_agent; ordering -created_at) + common migrations package (0001_initial, applied to the local Postgres dev DB). Service apps/common/bugs.py file_bug_report: strip + min 5 / max 4000, page ≤200, UA ≤300, rate limit 10/hour via common.rate_limit.assert_under_limit (RateLimitExceeded re-wrapped as BugReportError — stories.py convention). API api/v1/bugs.py POST /api/v1/bugs (BugIn {text, page=""} → BugOut {ok, id}; require_actor mints an anon session — no login needed; BugReportError → 400), router mounted in api/main.py. Admin: BugReportAdmin in common/admin.py — an inbox, deliberately NOT ReadOnlyAdmin (staff triage is the point): date_hierarchy + status filter, status list_editable, everything else readonly_fields, has_add_permission False (rows are API-written), delete left open for junk cleanup; reporter_label via display_pseudonym. Browser: More pane gets a BugReport row (ListRow asButton + hint subtitle) expanding an inline textarea form (send disabled under 5 chars, maxLength 4000) → api.reportBug(text, window.location.pathname) → success note replaces the form; own BugReport.module.css in the fund-card visual language; i18n more.reportBug* en/ru (+ types.ts). Mini-app skipped deliberately — it has no settings/More pane at all. Verify: manage.py check clean; apps/common pytest 34 passed incl. 4 new (account identity fields, anon session minted, validation 400s, rate limit); browser tsc -b + vite build ✓; vitest 69/69 (baseline kept).
files: backend/apps/common/{models.py,bugs.py (new),admin.py,migrations/__init__.py (new),migrations/0001_initial.py (new),tests/test_bugs.py (new)}, backend/api/v1/bugs.py (new), backend/api/main.py, apps/browser/src/features/more/{BugReport.tsx (new),BugReport.module.css (new),MorePane.tsx}, apps/browser/src/core/api/{client.ts,types.ts}, apps/browser/src/core/i18n/{types.ts,messages/en.ts,messages/ru.ts}, PROGRESS.md
api: POST /api/v1/bugs

