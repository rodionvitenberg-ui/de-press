# CONTEXT.md — moved

The canonical domain glossary lives at the repository root: [`CONTEXT.md`](../../CONTEXT.md).

This pointer keeps existing `de-press-docs/` links valid. Do not edit here —
edit the root file.
