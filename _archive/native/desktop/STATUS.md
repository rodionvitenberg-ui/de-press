# Native desktop — PAUSED

Workstation cannot complete tdesktop Docker build (RAM/disk/editor crashes).

**Active track:** `../android/` (ADR 0016).

tdesktop sources may remain under `tdesktop/` for later/CI machine.
Do **not** re-run `prepare/linux.sh` here without stronger hardware.
