# Legacy / archive

| Path | What |
|------|------|
| `next-frontend/` | Old Next.js UI (was root `frontend/`). **Not the main app.** Donor for types/logic only. |
| `next-frontend/_orphan_front_snippet.tsx` | Stray snippet that lived at repo root as `front`. |

Active UI: [`../apps/web/`](../apps/web/).
