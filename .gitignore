# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.sqlite3
db.sqlite3

# Django
backend/staticfiles/
backend/media/
*.log
local_settings.py

# Node
node_modules/
apps/browser/dist/
apps/mini-app/dist/
apps/admin/dist/
*.tsbuildinfo
next-env.d.ts

# Env & secrets
.env
.env.local
.env.*.local
!.env.example

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Docker volumes (if ever mounted into repo)
.pgdata/
.redisdata/

# Telegram Web A vendor (clone via scripts/fetch_telegram_tt.sh) — GPLv3, large
apps/mini-app/vendor/telegram-tt/

# The local archive (the legacy Next frontend, the native forks, AngelSlim) —
# kept on the dev disk only, not part of the published tree
_archive/

# SDD / worktrees
.superpowers/
.worktrees/

# RU docs archive (kept locally only, EN is canon — decision Q6 2026-08-30)
docs-ru-archive/

# Local agent skill library (published by mistake; not part of the product)
skills/

# Rendered deploy artifacts — produce them with deploy/render.sh
deploy/nginx-de-press.conf
deploy/depress-api.service
deploy/depress-celery.service
deploy/depress-celery-beat.service
deploy/depress-backup.service
deploy/depress-backup.timer
